import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:rabbited/app/modules/home/widgets/search_delegate/search_delegate_controller.dart';
import 'package:rabbited/app/modules/home/home_module.dart';

void main() {
  initModule(HomeModule());
  // SearchDelegateController searchdelegate;
  //
  setUp(() {
    //     searchdelegate = HomeModule.to.get<SearchDelegateController>();
  });

  group('SearchDelegateController Test', () {
    //   test("First Test", () {
    //     expect(searchdelegate, isInstanceOf<SearchDelegateController>());
    //   });

    //   test("Set Value", () {
    //     expect(searchdelegate.value, equals(0));
    //     searchdelegate.increment();
    //     expect(searchdelegate.value, equals(1));
    //   });
  });
}
