import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:rabbited/app/modules/home/widgets/anime_card/anime_card_controller.dart';
import 'package:rabbited/app/modules/home/home_module.dart';

void main() {
  initModule(HomeModule());
  // AnimeCardController animecard;
  //
  setUp(() {
    //     animecard = HomeModule.to.get<AnimeCardController>();
  });

  group('AnimeCardController Test', () {
    //   test("First Test", () {
    //     expect(animecard, isInstanceOf<AnimeCardController>());
    //   });

    //   test("Set Value", () {
    //     expect(animecard.value, equals(0));
    //     animecard.increment();
    //     expect(animecard.value, equals(1));
    //   });
  });
}
