import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:rabbited/app/modules/head/submodules/favorite/pages/collection/collection_page.dart';

void main() {
  testWidgets('CollectionPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CollectionPage(title: 'Collection')));
    //  final titleFinder = find.text('Collection');
    //  expect(titleFinder, findsOneWidget);
  });
}
