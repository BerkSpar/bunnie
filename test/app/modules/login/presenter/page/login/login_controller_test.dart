import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:rabbited/app/modules/login/presenter/page/login/login_controller.dart';
import 'package:rabbited/app/app_module.dart';

void main() {
  initModule(AppModule());
  // LoginController login;
  //
  setUp(() {
    //     login = AppModule.to.get<LoginController>();
  });

  group('LoginController Test', () {
    //   test("First Test", () {
    //     expect(login, isInstanceOf<LoginController>());
    //   });

    //   test("Set Value", () {
    //     expect(login.value, equals(0));
    //     login.increment();
    //     expect(login.value, equals(1));
    //   });
  });
}
