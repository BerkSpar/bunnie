import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:rabbited/app/modules/favorite/favorite_page.dart';

void main() {
  testWidgets('FavoritePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(FavoritePage(title: 'Favorite')));
    //  final titleFinder = find.text('Favorite');
    //  expect(titleFinder, findsOneWidget);
  });
}
